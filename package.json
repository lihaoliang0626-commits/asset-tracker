{
  "name": "asset-tracker",
  "version": "1.1.0",
  "private": true,
  "description": "Web-first asset tracking application with Supabase cloud storage",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "build:web": "pnpm --filter web build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "keywords": ["asset-tracking", "finance", "monorepo"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
