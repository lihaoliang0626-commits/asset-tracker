# 目标设定功能 - 完整设计方案

**版本**: 1.0
**日期**: 2026-02-02
**优先级**: P1（V1.1 功能）

---

## 一、功能概述

### 1.1 核心价值

帮助用户设定净资产目标，并基于历史数据提供**克制、不打扰**的进度追踪和趋势预测。

**设计原则**:
- ✅ **展示进度，不催促** - 只显示客观数据，不推送提醒
- ✅ **预测趋势，不承诺** - 基于历史数据推算，明确标注"仅供参考"
- ✅ **可选功能** - 用户可以不设置目标，不影响核心体验
- ❌ **不倒计时** - 避免"还差 XX 天"的焦虑设计
- ❌ **不强提醒** - 不推送"目标未完成"通知

### 1.2 用户场景

**典型用户故事**:
> 小李希望在 2027 年底前净资产达到 100 万。他每个月记录一次资产快照，想知道按照当前的趋势，是否能达成目标。但他不想被频繁提醒，只是希望在每次记录后，能看到客观的进度。

---

## 二、数据模型设计

### 2.1 目标（Goal）数据结构

创建新文件：`packages/shared/types/goal.ts`

```typescript
/**
 * 目标设定 - 数据模型
 */
export interface Goal {
  id: string;                      // 目标唯一ID
  userId: string;                  // 用户ID
  title: string;                   // 目标标题（如"2027年达到100万"）
  targetAmount: number;            // 目标金额（基准货币）
  baseCurrency: string;            // 基准货币
  deadline: number;                // 目标截止时间（时间戳）
  startAmount: number;             // 起始金额（设定目标时的净资产）
  startTime: number;               // 设定目标的时间（时间戳）
  description?: string;            // 目标描述/备注
  isActive: boolean;               // 是否激活（用户可以暂停目标）
  createdAt: number;               // 创建时间
  updatedAt: number;               // 更新时间
}

/**
 * 目标进度
 */
export interface GoalProgress {
  goalId: string;
  currentAmount: number;           // 当前净资产
  targetAmount: number;            // 目标金额
  startAmount: number;             // 起始金额
  progress: number;                // 进度百分比（0-100）
  remaining: number;               // 剩余金额

  // 时间进度
  elapsedDays: number;             // 已过天数
  totalDays: number;               // 总天数
  timeProgress: number;            // 时间进度百分比（0-100）

  // 趋势分析
  averageGrowthRate: number;       // 平均增长率（每月）
  predictedAmount?: number;        // 预测到期金额
  predictedDate?: number;          // 预测达成日期（如果按当前趋势）
  onTrack: boolean;                // 是否在正轨上

  calculatedAt: number;            // 计算时间
}

/**
 * 目标创建输入
 */
export interface CreateGoalInput {
  userId: string;
  title: string;
  targetAmount: number;
  deadline: number;                // 截止日期（时间戳）
  description?: string;
}

/**
 * 目标更新输入
 */
export interface UpdateGoalInput {
  id: string;
  title?: string;
  targetAmount?: number;
  deadline?: number;
  description?: string;
  isActive?: boolean;
}

/**
 * 目标达成状态
 */
export type GoalStatus =
  | 'not_started'    // 尚未开始（当前金额 < 起始金额）
  | 'in_progress'    // 进行中
  | 'on_track'       // 进行中且在正轨上
  | 'behind'         // 进行中但落后
  | 'achieved'       // 已达成
  | 'overdue';       // 已过期但未达成
```

### 2.2 数据库存储结构

```typescript
// IndexedDB / SQLite 表结构
interface GoalsTable {
  goals: Goal[];              // 目标表
  goalProgress: GoalProgress[]; // 进度缓存表（可选）
}

// 索引
{
  goals: {
    keyPath: 'id',
    indexes: [
      { name: 'userId', keyPath: 'userId' },
      { name: 'deadline', keyPath: 'deadline' },
      { name: 'isActive', keyPath: 'isActive' },
      { name: 'userActive', keyPath: ['userId', 'isActive'] }
    ]
  }
}
```

---

## 三、核心算法设计

### 3.1 进度计算算法

```typescript
/**
 * 计算目标进度
 */
export function calculateGoalProgress(
  goal: Goal,
  currentSnapshot: Snapshot,
  historicalSnapshots: Snapshot[]
): GoalProgress {
  const currentAmount = currentSnapshot.totalAsset;
  const { targetAmount, startAmount, startTime, deadline } = goal;

  // 1. 基础进度计算
  const totalChange = targetAmount - startAmount;
  const currentChange = currentAmount - startAmount;
  const progress = totalChange === 0
    ? 100
    : Math.min(100, Math.max(0, (currentChange / totalChange) * 100));

  const remaining = Math.max(0, targetAmount - currentAmount);

  // 2. 时间进度
  const now = Date.now();
  const elapsedDays = Math.floor((now - startTime) / (1000 * 60 * 60 * 24));
  const totalDays = Math.floor((deadline - startTime) / (1000 * 60 * 60 * 24));
  const timeProgress = totalDays === 0
    ? 100
    : Math.min(100, (elapsedDays / totalDays) * 100);

  // 3. 趋势预测
  const prediction = predictGoalAchievement(
    goal,
    currentAmount,
    historicalSnapshots
  );

  // 4. 判断是否在正轨上
  // 如果实际进度 >= 时间进度的 90%，认为在正轨上
  const onTrack = progress >= timeProgress * 0.9;

  return {
    goalId: goal.id,
    currentAmount,
    targetAmount,
    startAmount,
    progress,
    remaining,
    elapsedDays,
    totalDays,
    timeProgress,
    averageGrowthRate: prediction.averageMonthlyGrowth,
    predictedAmount: prediction.predictedAmount,
    predictedDate: prediction.predictedDate,
    onTrack,
    calculatedAt: now
  };
}
```

### 3.2 趋势预测算法

```typescript
/**
 * 预测目标达成情况
 */
function predictGoalAchievement(
  goal: Goal,
  currentAmount: number,
  historicalSnapshots: Snapshot[]
): {
  averageMonthlyGrowth: number;
  predictedAmount: number;
  predictedDate?: number;
} {
  // 1. 筛选目标设定后的快照
  const relevantSnapshots = historicalSnapshots
    .filter(s => s.timestamp >= goal.startTime)
    .sort((a, b) => a.timestamp - b.timestamp);

  // 如果数据不足（少于2个快照），无法预测
  if (relevantSnapshots.length < 2) {
    return {
      averageMonthlyGrowth: 0,
      predictedAmount: currentAmount,
      predictedDate: undefined
    };
  }

  // 2. 计算月均增长率（线性回归）
  const growthRates = calculateGrowthRates(relevantSnapshots);
  const averageMonthlyGrowth = calculateAverageMonthlyGrowth(growthRates);

  // 3. 预测到截止日期的金额
  const now = Date.now();
  const remainingMonths = (goal.deadline - now) / (1000 * 60 * 60 * 24 * 30);
  const predictedGrowth = averageMonthlyGrowth * remainingMonths;
  const predictedAmount = currentAmount + predictedGrowth;

  // 4. 预测达成日期（如果趋势持续）
  let predictedDate: number | undefined;
  if (averageMonthlyGrowth > 0) {
    const remainingAmount = goal.targetAmount - currentAmount;
    const monthsNeeded = remainingAmount / averageMonthlyGrowth;
    predictedDate = now + (monthsNeeded * 30 * 24 * 60 * 60 * 1000);
  }

  return {
    averageMonthlyGrowth,
    predictedAmount,
    predictedDate
  };
}

/**
 * 计算增长率数组
 */
function calculateGrowthRates(snapshots: Snapshot[]): number[] {
  const growthRates: number[] = [];

  for (let i = 1; i < snapshots.length; i++) {
    const prev = snapshots[i - 1];
    const curr = snapshots[i];

    const timeDiff = curr.timestamp - prev.timestamp;
    const monthsDiff = timeDiff / (1000 * 60 * 60 * 24 * 30);

    if (monthsDiff > 0) {
      const absoluteGrowth = curr.totalAsset - prev.totalAsset;
      const monthlyGrowth = absoluteGrowth / monthsDiff;
      growthRates.push(monthlyGrowth);
    }
  }

  return growthRates;
}

/**
 * 计算平均月增长率（加权平均，近期权重更高）
 */
function calculateAverageMonthlyGrowth(growthRates: number[]): number {
  if (growthRates.length === 0) return 0;

  // 使用指数加权移动平均（EWMA）
  // 近期数据权重更高
  const alpha = 0.3; // 平滑系数
  let ewma = growthRates[0];

  for (let i = 1; i < growthRates.length; i++) {
    ewma = alpha * growthRates[i] + (1 - alpha) * ewma;
  }

  return ewma;
}

/**
 * 获取目标状态
 */
export function getGoalStatus(
  goal: Goal,
  progress: GoalProgress
): GoalStatus {
  const now = Date.now();

  // 已达成
  if (progress.currentAmount >= goal.targetAmount) {
    return 'achieved';
  }

  // 已过期但未达成
  if (now > goal.deadline) {
    return 'overdue';
  }

  // 尚未开始
  if (progress.currentAmount < goal.startAmount) {
    return 'not_started';
  }

  // 进行中 - 判断是否在正轨上
  if (progress.onTrack) {
    return 'on_track';
  } else {
    return 'behind';
  }
}
```

---

## 四、UI/UX 设计方案

### 4.1 设置目标界面

**位置**: 总览页 → "设置目标" 按钮

**设计要点**:
```
┌─────────────────────────────────────┐
│  设置资产目标                        │
├─────────────────────────────────────┤
│                                     │
│  目标名称                            │
│  ┌─────────────────────────────┐   │
│  │ 2027年达到100万              │   │
│  └─────────────────────────────┘   │
│  例如："30岁前存到50万"             │
│                                     │
│  目标金额                            │
│  ┌─────────────────────────────┐   │
│  │ 1,000,000                   │CNY│
│  └─────────────────────────────┘   │
│                                     │
│  截止日期                            │
│  ┌─────────────────────────────┐   │
│  │ 2027-12-31                  │   │
│  └─────────────────────────────┘   │
│                                     │
│  描述（可选）                        │
│  ┌─────────────────────────────┐   │
│  │ 为了...                      │   │
│  └─────────────────────────────┘   │
│                                     │
│  💡 提示：                          │
│  - 目标只是参考，不会打扰你          │
│  - 你可以随时修改或暂停目标          │
│  - 我们会基于历史数据预测趋势        │
│                                     │
│  ┌─────────────┐  ┌─────────────┐  │
│  │   取消      │  │  设置目标    │  │
│  └─────────────┘  └─────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

**交互细节**:
- 目标金额输入时自动格式化（千分位）
- 截止日期不能早于当前日期
- 所有字段都可选填，但建议填写完整
- 柔和的颜色，避免红绿配色

### 4.2 目标进度卡片（总览页）

**位置**: 总览页顶部，在"当前资产"下方

**设计方案 A：简洁版（推荐）**

```
┌─────────────────────────────────────┐
│  🎯 目标：2027年达到100万            │
│                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│  58.5%                              │
│                                     │
│  当前 58.5万 / 目标 100万            │
│  还需 41.5万                         │
│                                     │
│  ⏱ 时间进度：45%（已过 18/40 个月） │
│                                     │
│  📈 按当前趋势，预计 2027年10月达成   │
│     （仅供参考，基于近期增长率）     │
│                                     │
│  ┌─────────────┐                    │
│  │  查看详情   │                    │
│  └─────────────┘                    │
└─────────────────────────────────────┘
```

**设计方案 B：可视化版**

```
┌─────────────────────────────────────┐
│  🎯 2027年达到100万                  │
│                                     │
│  当前进度                            │
│  ┌─────────────────────────────┐   │
│  │ ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░         │   │
│  │ 58.5%                       │   │
│  └─────────────────────────────┘   │
│  58.5万 / 100万                     │
│                                     │
│  时间进度                            │
│  ┌─────────────────────────────┐   │
│  │ ▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░    │   │
│  │ 45%                         │   │
│  └─────────────────────────────┘   │
│  18个月 / 40个月                    │
│                                     │
│  ┌────────────────────────────┐    │
│  │ 💡 你的进度超过时间进度！    │    │
│  │ 按当前趋势，预计提前2个月达成│    │
│  └────────────────────────────┘    │
│                                     │
│  ┌─────────────┐                    │
│  │  查看详情   │                    │
│  └─────────────┘                    │
└─────────────────────────────────────┘
```

**配色方案**（克制设计）:
- 进度条：深蓝色（#2563EB）
- 背景：浅灰色（#F9FAFB）
- 文字：深灰色（#374151）
- ✅ 不使用红色（避免焦虑）
- ✅ 不使用绿色（避免情绪暗示）

### 4.3 目标详情页

**位置**: 点击"查看详情"后进入

```
┌─────────────────────────────────────┐
│  ← 返回          🎯 目标详情        ⋮│
├─────────────────────────────────────┤
│                                     │
│  2027年达到100万                     │
│  设定于 2024-01-15                   │
│                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                     │
│  【进度趋势图】                      │
│  ┌─────────────────────────────┐   │
│  │  100万 ┐                    │   │
│  │        │    ╱预测线          │   │
│  │  75万  │   ╱                 │   │
│  │        │  ╱                  │   │
│  │  50万  │ ●●●● 实际线         │   │
│  │        │●                    │   │
│  │  25万  ●                     │   │
│  │        │                     │   │
│  │   0    └────────────────→    │   │
│  │      2024  2025  2026  2027  │   │
│  └─────────────────────────────┘   │
│                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                     │
│  📊 进度概览                         │
│                                     │
│  当前资产     58.5万                 │
│  目标金额     100万                  │
│  起始金额     35万                   │
│  已完成       58.5%                  │
│  还需         41.5万                 │
│                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                     │
│  ⏱ 时间分析                         │
│                                     │
│  已过时间     18个月                 │
│  总时长       40个月                 │
│  时间进度     45%                    │
│  剩余时间     22个月                 │
│  截止日期     2027-12-31             │
│                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                     │
│  📈 趋势预测                         │
│                                     │
│  月均增长     +1.3万 / 月            │
│  预计到期金额  105.6万               │
│  预测达成日期  2027年10月            │
│                                     │
│  💡 基于最近12个月的数据推算         │
│  仅供参考，实际情况可能有所不同      │
│                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                     │
│  ┌─────────────┐  ┌─────────────┐  │
│  │  暂停目标   │  │  编辑目标    │  │
│  └─────────────┘  └─────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

**图表设计要点**:
- 使用 Recharts 折线图
- 实际线：实线 + 圆点标记
- 预测线：虚线 + 不同颜色（浅蓝）
- 目标线：水平虚线
- 平滑曲线，视觉舒适

### 4.4 快照记录后的目标反馈

**场景**: 用户刚记录完新快照

**设计**（模态框或卡片）:

```
┌─────────────────────────────────────┐
│  ✓ 快照已保存                       │
│                                     │
│  🎯 目标进度更新                     │
│                                     │
│  你的净资产从 56.2万 增加到 58.5万   │
│                                     │
│  目标完成度：55% → 58.5%             │
│  ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░               │
│                                     │
│  💡 很好！你超过了预期进度           │
│  按当前趋势，可能提前达成目标        │
│                                     │
│  ┌─────────────┐                    │
│  │    知道了   │                    │
│  └─────────────┘                    │
└─────────────────────────────────────┘
```

**文案原则**:
- ✅ "很好" 而非 "太棒了"（克制）
- ✅ "可能提前" 而非 "一定能"（不承诺）
- ✅ 中性描述，不过度鼓励或批评
- ❌ 避免"加油"、"努力"等催促性文案

---

## 五、技术实现

### 5.1 类型定义文件

创建 `packages/shared/types/goal.ts`（见第二章）

### 5.2 数据访问层（DAL）

创建 `packages/shared/utils/goal-service.ts`

```typescript
import { Goal, GoalProgress, CreateGoalInput, UpdateGoalInput } from '../types/goal';
import { Snapshot } from '../types/snapshot';
import { db } from './db'; // 假设有统一的数据库访问层

/**
 * 目标服务
 */
export class GoalService {
  /**
   * 创建目标
   */
  static async createGoal(input: CreateGoalInput, currentSnapshot: Snapshot): Promise<Goal> {
    const now = Date.now();

    const goal: Goal = {
      id: `goal_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,
      userId: input.userId,
      title: input.title,
      targetAmount: input.targetAmount,
      baseCurrency: currentSnapshot.baseCurrency,
      deadline: input.deadline,
      startAmount: currentSnapshot.totalAsset,
      startTime: now,
      description: input.description,
      isActive: true,
      createdAt: now,
      updatedAt: now
    };

    await db.goals.add(goal);
    return goal;
  }

  /**
   * 更新目标
   */
  static async updateGoal(input: UpdateGoalInput): Promise<Goal> {
    const goal = await db.goals.get(input.id);
    if (!goal) {
      throw new Error('Goal not found');
    }

    const updated: Goal = {
      ...goal,
      ...(input.title && { title: input.title }),
      ...(input.targetAmount && { targetAmount: input.targetAmount }),
      ...(input.deadline && { deadline: input.deadline }),
      ...(input.description !== undefined && { description: input.description }),
      ...(input.isActive !== undefined && { isActive: input.isActive }),
      updatedAt: Date.now()
    };

    await db.goals.put(updated);
    return updated;
  }

  /**
   * 获取用户的活跃目标
   */
  static async getActiveGoal(userId: string): Promise<Goal | null> {
    const goals = await db.goals
      .where({ userId, isActive: true })
      .sortBy('createdAt');

    return goals.length > 0 ? goals[0] : null;
  }

  /**
   * 计算目标进度
   */
  static async calculateProgress(
    goalId: string,
    currentSnapshot: Snapshot
  ): Promise<GoalProgress> {
    const goal = await db.goals.get(goalId);
    if (!goal) {
      throw new Error('Goal not found');
    }

    // 获取历史快照
    const historicalSnapshots = await db.snapshots
      .where('userId')
      .equals(goal.userId)
      .and(s => s.timestamp >= goal.startTime)
      .sortBy('timestamp');

    return calculateGoalProgress(goal, currentSnapshot, historicalSnapshots);
  }

  /**
   * 删除目标
   */
  static async deleteGoal(goalId: string): Promise<void> {
    await db.goals.delete(goalId);
  }
}
```

### 5.3 UI 组件

创建 `packages/web/src/components/goal/GoalProgressCard.tsx`

```typescript
import React from 'react';
import { Goal, GoalProgress, GoalStatus } from '@asset-tracker/shared/types/goal';
import { getGoalStatus } from '@asset-tracker/shared/utils/goal-calculator';

interface GoalProgressCardProps {
  goal: Goal;
  progress: GoalProgress;
  onViewDetails: () => void;
}

export const GoalProgressCard: React.FC<GoalProgressCardProps> = ({
  goal,
  progress,
  onViewDetails
}) => {
  const status = getGoalStatus(goal, progress);

  // 格式化金额
  const formatAmount = (amount: number) => {
    return new Intl.NumberFormat('zh-CN', {
      style: 'currency',
      currency: goal.baseCurrency,
      minimumFractionDigits: 0,
      maximumFractionDigits: 0
    }).format(amount);
  };

  // 格式化日期
  const formatDate = (timestamp: number) => {
    return new Intl.DateTimeFormat('zh-CN', {
      year: 'numeric',
      month: 'long'
    }).format(new Date(timestamp));
  };

  // 状态文案
  const getStatusText = () => {
    if (status === 'achieved') {
      return '🎉 目标已达成';
    }
    if (status === 'overdue') {
      return '⏰ 已过期';
    }
    if (status === 'on_track') {
      return '💡 进度良好，超过时间预期';
    }
    if (status === 'behind') {
      return '📊 按当前趋势，可能需要更长时间';
    }
    return '';
  };

  // 预测文案
  const getPredictionText = () => {
    if (!progress.predictedAmount || status === 'achieved') {
      return null;
    }

    const willAchieve = progress.predictedAmount >= goal.targetAmount;

    if (willAchieve && progress.predictedDate) {
      const predictedDateStr = formatDate(progress.predictedDate);
      const isEarly = progress.predictedDate < goal.deadline;

      if (isEarly) {
        return `按当前趋势，预计 ${predictedDateStr} 达成`;
      } else {
        return `按当前趋势，预计 ${predictedDateStr} 达成`;
      }
    }

    return `按当前趋势，到期预计达到 ${formatAmount(progress.predictedAmount)}`;
  };

  return (
    <div className="bg-white rounded-lg shadow-sm p-6 border border-gray-200">
      {/* 标题 */}
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-lg font-medium text-gray-900">
          🎯 {goal.title}
        </h3>
      </div>

      {/* 进度条 */}
      <div className="mb-4">
        <div className="flex justify-between text-sm text-gray-600 mb-2">
          <span>当前进度</span>
          <span className="font-medium">{progress.progress.toFixed(1)}%</span>
        </div>
        <div className="w-full bg-gray-200 rounded-full h-3">
          <div
            className="bg-blue-600 h-3 rounded-full transition-all duration-500"
            style={{ width: `${Math.min(100, progress.progress)}%` }}
          />
        </div>
      </div>

      {/* 金额信息 */}
      <div className="flex justify-between text-sm mb-4">
        <div>
          <div className="text-gray-600">当前</div>
          <div className="font-medium text-gray-900">
            {formatAmount(progress.currentAmount)}
          </div>
        </div>
        <div className="text-right">
          <div className="text-gray-600">目标</div>
          <div className="font-medium text-gray-900">
            {formatAmount(goal.targetAmount)}
          </div>
        </div>
      </div>

      {/* 时间进度 */}
      <div className="mb-4">
        <div className="flex justify-between text-sm text-gray-600 mb-1">
          <span>时间进度</span>
          <span>{progress.timeProgress.toFixed(0)}%</span>
        </div>
        <div className="text-xs text-gray-500">
          已过 {progress.elapsedDays} / {progress.totalDays} 天
        </div>
      </div>

      {/* 状态提示 */}
      {status !== 'in_progress' && (
        <div className={`
          px-3 py-2 rounded-md text-sm mb-4
          ${status === 'achieved' ? 'bg-blue-50 text-blue-700' : ''}
          ${status === 'on_track' ? 'bg-blue-50 text-blue-700' : ''}
          ${status === 'behind' ? 'bg-gray-100 text-gray-700' : ''}
          ${status === 'overdue' ? 'bg-gray-100 text-gray-700' : ''}
        `}>
          {getStatusText()}
        </div>
      )}

      {/* 预测 */}
      {getPredictionText() && (
        <div className="text-sm text-gray-600 mb-4">
          📈 {getPredictionText()}
          <div className="text-xs text-gray-500 mt-1">
            （仅供参考，基于近期增长率）
          </div>
        </div>
      )}

      {/* 查看详情按钮 */}
      <button
        onClick={onViewDetails}
        className="w-full py-2 px-4 border border-gray-300 rounded-md text-sm font-medium text-gray-700 hover:bg-gray-50 transition-colors"
      >
        查看详情
      </button>
    </div>
  );
};
```

### 5.4 趋势图组件

创建 `packages/web/src/components/goal/GoalTrendChart.tsx`

```typescript
import React from 'react';
import {
  LineChart,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
  ReferenceLine
} from 'recharts';
import { Goal, GoalProgress } from '@asset-tracker/shared/types/goal';
import { Snapshot } from '@asset-tracker/shared/types/snapshot';

interface GoalTrendChartProps {
  goal: Goal;
  progress: GoalProgress;
  snapshots: Snapshot[];
}

export const GoalTrendChart: React.FC<GoalTrendChartProps> = ({
  goal,
  progress,
  snapshots
}) => {
  // 准备图表数据
  const chartData = prepareChartData(goal, progress, snapshots);

  // 格式化金额
  const formatAmount = (value: number) => {
    return (value / 10000).toFixed(1) + '万';
  };

  // 格式化日期
  const formatDate = (timestamp: number) => {
    return new Intl.DateTimeFormat('zh-CN', {
      year: 'numeric',
      month: 'short'
    }).format(new Date(timestamp));
  };

  return (
    <div className="w-full h-80">
      <ResponsiveContainer width="100%" height="100%">
        <LineChart data={chartData}>
          <CartesianGrid strokeDasharray="3 3" stroke="#E5E7EB" />

          <XAxis
            dataKey="date"
            tickFormatter={formatDate}
            stroke="#6B7280"
          />

          <YAxis
            tickFormatter={formatAmount}
            stroke="#6B7280"
          />

          <Tooltip
            formatter={(value: number) => [formatAmount(value), '']}
            labelFormatter={formatDate}
          />

          <Legend />

          {/* 目标线 */}
          <ReferenceLine
            y={goal.targetAmount}
            stroke="#9CA3AF"
            strokeDasharray="5 5"
            label="目标"
          />

          {/* 实际资产线 */}
          <Line
            type="monotone"
            dataKey="actual"
            stroke="#2563EB"
            strokeWidth={2}
            dot={{ fill: '#2563EB', r: 4 }}
            name="实际资产"
          />

          {/* 预测线 */}
          <Line
            type="monotone"
            dataKey="predicted"
            stroke="#93C5FD"
            strokeWidth={2}
            strokeDasharray="5 5"
            dot={false}
            name="趋势预测"
          />
        </LineChart>
      </ResponsiveContainer>
    </div>
  );
};

/**
 * 准备图表数据
 */
function prepareChartData(
  goal: Goal,
  progress: GoalProgress,
  snapshots: Snapshot[]
) {
  const data: Array<{
    date: number;
    actual?: number;
    predicted?: number;
  }> = [];

  // 1. 添加起点
  data.push({
    date: goal.startTime,
    actual: goal.startAmount
  });

  // 2. 添加历史快照
  snapshots
    .filter(s => s.timestamp >= goal.startTime)
    .forEach(s => {
      data.push({
        date: s.timestamp,
        actual: s.totalAsset
      });
    });

  // 3. 添加预测点
  if (progress.predictedAmount && progress.averageGrowthRate > 0) {
    const now = Date.now();
    const monthsToDeadline = (goal.deadline - now) / (1000 * 60 * 60 * 24 * 30);

    // 每个月添加一个预测点
    for (let i = 1; i <= Math.ceil(monthsToDeadline); i++) {
      const futureDate = now + (i * 30 * 24 * 60 * 60 * 1000);
      const predictedValue = progress.currentAmount + (progress.averageGrowthRate * i);

      if (futureDate <= goal.deadline) {
        data.push({
          date: futureDate,
          predicted: predictedValue
        });
      }
    }
  }

  return data.sort((a, b) => a.date - b.date);
}
```

---

## 六、完整实现步骤

### Step 1: 添加类型定义

```bash
# 创建目标相关类型
touch packages/shared/types/goal.ts
```

将第二章的类型定义代码粘贴进去。

### Step 2: 更新数据库 schema

编辑 `packages/shared/utils/db.ts`（或相应的数据库配置文件）

```typescript
// 添加 goals 表
export const schema = {
  // ... 现有表
  goals: {
    keyPath: 'id',
    indexes: [
      { name: 'userId', keyPath: 'userId' },
      { name: 'isActive', keyPath: 'isActive' },
      { name: 'deadline', keyPath: 'deadline' }
    ]
  }
};
```

### Step 3: 实现核心算法

```bash
# 创建目标计算工具
touch packages/shared/utils/goal-calculator.ts
```

将第三章的算法代码实现。

### Step 4: 实现数据服务

```bash
# 创建目标服务
touch packages/shared/utils/goal-service.ts
```

将 5.2 节的服务代码实现。

### Step 5: 创建 UI 组件

```bash
# Web 组件
mkdir -p packages/web/src/components/goal
touch packages/web/src/components/goal/GoalProgressCard.tsx
touch packages/web/src/components/goal/GoalTrendChart.tsx
touch packages/web/src/components/goal/GoalSetupModal.tsx
touch packages/web/src/components/goal/GoalDetailPage.tsx
```

### Step 6: 集成到总览页

编辑 `packages/web/src/pages/overview/OverviewPage.tsx`

```typescript
import { GoalProgressCard } from '@/components/goal/GoalProgressCard';
import { GoalService } from '@asset-tracker/shared/utils/goal-service';

export function OverviewPage() {
  const [goal, setGoal] = useState<Goal | null>(null);
  const [goalProgress, setGoalProgress] = useState<GoalProgress | null>(null);

  useEffect(() => {
    loadGoal();
  }, []);

  const loadGoal = async () => {
    const activeGoal = await GoalService.getActiveGoal(userId);
    if (activeGoal) {
      setGoal(activeGoal);
      const progress = await GoalService.calculateProgress(
        activeGoal.id,
        currentSnapshot
      );
      setGoalProgress(progress);
    }
  };

  return (
    <div>
      {/* 现有内容 */}

      {/* 目标进度卡片 */}
      {goal && goalProgress && (
        <GoalProgressCard
          goal={goal}
          progress={goalProgress}
          onViewDetails={() => router.push('/goal/details')}
        />
      )}

      {/* 设置目标按钮 */}
      {!goal && (
        <button onClick={() => setShowGoalSetup(true)}>
          🎯 设置资产目标
        </button>
      )}
    </div>
  );
}
```

### Step 7: 测试

编写单元测试：

```bash
touch packages/shared/utils/__tests__/goal-calculator.test.ts
```

```typescript
import { calculateGoalProgress, predictGoalAchievement } from '../goal-calculator';

describe('Goal Calculator', () => {
  it('should calculate progress correctly', () => {
    const goal = {
      targetAmount: 1000000,
      startAmount: 500000,
      // ...
    };

    const currentSnapshot = {
      totalAsset: 750000,
      // ...
    };

    const progress = calculateGoalProgress(goal, currentSnapshot, []);

    expect(progress.progress).toBe(50); // (750000 - 500000) / (1000000 - 500000) * 100
  });

  // 更多测试...
});
```

---

## 七、注意事项与最佳实践

### 7.1 保持克制的设计原则

1. **不要过度鼓励**
   - ❌ "太棒了！你已经完成了58%！"
   - ✅ "当前完成度：58%"

2. **不要催促**
   - ❌ "还差 41.5万，加油！"
   - ✅ "还需 41.5万"

3. **预测要谨慎**
   - ✅ 始终标注"仅供参考"
   - ✅ 基于历史数据说明
   - ✅ 不承诺一定达成

4. **失败不批评**
   - ❌ "你落后了，需要努力"
   - ✅ "按当前趋势，可能需要更长时间"

### 7.2 数据准确性

1. **最少数据要求**
   - 至少2个快照才能预测
   - 不足时显示"数据不足，无法预测"

2. **异常值处理**
   - 剔除明显异常的快照
   - 或使用中位数而非平均值

3. **预测置信度**
   - 快照越多，预测越准
   - 可显示置信度百分比

### 7.3 性能优化

1. **缓存进度计算**
   - 避免每次都重新计算
   - 快照更新后才刷新

2. **懒加载图表**
   - 详情页才加载 Recharts
   - 减少首屏加载时间

3. **数据分页**
   - 历史快照太多时分页加载

### 7.4 边界情况处理

1. **目标金额 < 当前资产**
   - 显示"已达成"
   - 不显示进度条（已100%）

2. **负增长**
   - 预测文案："按当前趋势，到期可能无法达成"
   - 不显示预测达成日期

3. **目标已过期**
   - 状态显示"已过期"
   - 仍可查看进度和分析
   - 提示"可以编辑或创建新目标"

---

## 八、扩展功能（可选）

### 8.1 多目标支持

允许用户设置多个目标（如短期、中期、长期）

```typescript
interface Goal {
  // ... 现有字段
  priority: 'primary' | 'secondary'; // 主要/次要目标
}
```

### 8.2 目标里程碑

支持阶段性目标

```typescript
interface Milestone {
  goalId: string;
  amount: number;
  deadline: number;
  achieved: boolean;
}
```

### 8.3 目标模板

预设常见目标

```typescript
const goalTemplates = [
  { title: '30岁前存到50万', amount: 500000 },
  { title: '3年内买房首付', amount: 1000000 },
  // ...
];
```

---

## 九、总结

### 9.1 核心优势

1. ✅ **完全契合应用理念** - 低频、克制、不焦虑
2. ✅ **技术实现简单** - 基于现有数据模型扩展
3. ✅ **用户价值明确** - 赋予记录更多意义
4. ✅ **差异化明显** - 竞品少有如此克制的目标功能

### 9.2 实施建议

**Phase 1（MVP）**:
- 单一目标支持
- 基础进度展示
- 简单趋势预测

**Phase 2（优化）**:
- 可视化图表
- 详情页完善
- 多目标支持

**Phase 3（高级）**:
- 里程碑功能
- AI 预测优化
- 导出目标报告

### 9.3 预期效果

- **提升留存率** - 目标让用户有持续记录的动力
- **增加粘性** - 定期查看进度成为习惯
- **保持克制** - 不催促，符合产品定位

---

**参考资料**:
- [Recharts 官方文档](https://recharts.org/)
- [EWMA 算法](https://en.wikipedia.org/wiki/Moving_average#Exponential_moving_average)
- [UX 心理学 - 避免焦虑设计](https://www.onething.design/post/top-10-fintech-ux-design-practices-2026)
